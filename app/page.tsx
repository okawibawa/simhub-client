"use client";

// atoms
import { Button, Input, Anchor, Typography } from "./_components/atoms";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24 bg-zinc-800">
      <Typography as="body7">this is a typography component.</Typography>
    </main>
  );
}
